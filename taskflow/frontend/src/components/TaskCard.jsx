import { CalendarDays, MoreHorizontal, UserRound } from 'lucide-react';

export default function TaskCard({ task, onMove }) { return <article className="task-card"><div className="task-top"><span className={`priority ${task.priority}`}>{task.priority}</span><button className="icon-button" aria-label="Task actions"><MoreHorizontal size={17}/></button></div><h3>{task.title}</h3>{task.description && <p>{task.description}</p>}<div className="task-meta">{task.dueDate && <span><CalendarDays size={14}/> {new Date(task.dueDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}</span>}{task.assignee ? <span className="assignee"><UserRound size={14}/> {task.assignee.name}</span> : <button className="assign-button"><UserRound size={14}/> Assign</button>}</div><select className="move-select" value={task.status} onChange={e => onMove(task._id, e.target.value)} aria-label="Move task"><option value="backlog">Backlog</option><option value="inProgress">In progress</option><option value="review">Review</option><option value="done">Done</option></select></article>; }
